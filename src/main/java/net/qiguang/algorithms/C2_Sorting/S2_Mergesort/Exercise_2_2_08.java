package net.qiguang.algorithms.C2_Sorting.S2_Mergesort;

/**
 * 2.2.8
 * Suppose that Algorithm 2.4 is modified to skip the call on merge() whenever a[mid] <= a[mid+1].
 * Prove that the number of compares used to mergesort a sorted array is linear.
 *
 * Answer:
 * Each attempt to call merge only requires 2 array accesses, and merge is never invoked.
 */
public class Exercise_2_2_08 {
    // Nothing to see here.
}
